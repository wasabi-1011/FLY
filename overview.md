# FLY 品牌官网项目 · 交付总览

> 创建日期：2026-09-07 ｜ 状态：PRD **v1.6 评审修订版**（v1.2 评审修复 + v1.3 首页视觉对齐 + v1.4 后台图片/轮播/文字可配置澄清 + **v1.5 数据库选型定稿为 SQLite** + **v1.6 款式新增价格记录字段**）

## 一、本次交付

| 文件 | 说明 |
|---|---|
| `docs/FLY品牌官网_PRD_v1.0.md` | 主交付物（内容现为 **v1.6 评审修订版**，文件名待统一）。含 13 章 + 2 附录；v1.2 评审修复 30+、v1.3 首页视觉对齐、v1.4 后台可配置澄清、v1.5 数据库选型定稿 SQLite、v1.6 款式新增 `price` 价格记录字段（后端不读写） |
| `docs/FLY品牌官网_PRD_审核报告_v1.2.md` | **新增**：评审报告，按严重/中等/轻微/建议四级列出全部问题、处置与遗留决策 |
| `docs/FLY品牌官网_数据库设计技术文档_SQLite_v1.0.md` | **新增（v1.5）/ v1.1 更新**：数据库设计技术文档——12 张表全部存储对象、字段类型（SQLite）、约束索引、枚举字典、DDL；v1.1 起款式 `items` 新增 `price` 记录字段（后端不读写、前台不展示） |
| `docs/FLY官网_SQLite建表DDL.sql` | **新增（v1.5）/ v1.1 更新**：由 SQLAlchemy 编译导出的完整可执行建表脚本（v1.1 起手工补入 `items.price` 记录列，脚本内已注释标注） |
| `overview.md` | 本文件，项目总览与决策记录 |

## 二、产品定位（一句话）

**FLY 品牌官方网站**——以品牌内容为核心、不做在线交易的纯展示型官网。前端 React SSR（利于 SEO），后台 React SPA + Ant Design，服务端 FastAPI。参考 PEACEBIRD 太平鸟官网的品牌叙事逻辑，但不复刻其内容贫瘠与 SEO 缺陷。

## 三、需求澄清结论（三轮对齐）

### 第一轮：定位与范围
- 站点定位 → **纯品牌展示官网**（非电商、非交易平台）
- 前台模块 → 商品中心、品牌内容、会员与门店（其中门店要求带**全国地图**）
- 后台模块 → 商品与库存、订单与售后
- 技术形态 → **前台 React SSR + 后台 React SPA + Ant Design**

### 第二轮：解决三处冲突
| 冲突 | 结论 |
|---|---|
| 纯展示官网却要"订单与售后" | **删除**订单与售后模块，后台保留商品与库存 |
| 纯展示却要会员体系 | **删除**会员系统，全站匿名浏览、无登录态 |
| 商品粒度不明 | 采用**系列/款式**两级，无 SKU、无价格、无尺码、无加购 |
| 品牌主体未定 | 品牌名 **FLY**（自有品牌） |

### 第三轮：收尾
- 库存语义 → **门店有货查询**（内部工具，前台不展示数量）
- 内容更新 → **后台 CMS 可视化装修**（否则官网变"死站"）
- 附加章节 → 不做独立章节（仅保留参考站点分析与精简数据模型）
- 交付格式 → **Markdown**

## 四、默认假设（用户未明确、由我拍板，可推翻）

1. 门店地图用**高德地图 JS API**（国内合规、有审图号）
2. 后台**轻量 RBAC 三角色**：超级管理员 / 内容运营 / 商品门店运营
3. 门店数据用**示例数据**（8 城约 20 家：广州、武汉、深圳、上海、北京、成都、杭州、西安），上线前替换
4. 一期**仅中文**，i18n 架构预留
5. 工期与团队规模写 `[TBD]`

## 五、明确不做（Non-goals，防止范围蔓延）

在线下单与支付、购物车、订单与售后、会员注册登录与积分、商品评价、SKU 维度信息前台展示、多商户/加盟商、APP 与小程序、多语言、ERP/OMS 对接、AR 试穿与 AI 推荐、在线客服 IM。

## 六、关键决策与理由

| 决策 | 理由 |
|---|---|
| 前台必须 SSR | 太平鸟官网用 hash 路由 SPA，SEO 不可达；FLY 是新品牌，搜索收录是生命线 |
| 前后台拆成两个应用 | 官网要 SSR + 极致性能，后台要开发效率，混在一起两边都不讨好 |
| 首期就做 CMS | 换季上新是最高频动作，硬编码会导致每次改版都发版 |
| 商品用"系列/款式"而非 SKU | 保持品牌叙事感，避免官网变成货架 |
| 学的是叙事逻辑不是表面形态 | 太平鸟靠已有知名度可以极简，FLY 需要更多可被收录的内容页 |
| **数据库定稿 SQLite（v1.5）** | 纯展示内容站、数据量小、读多写少；单文件零运维、备份即复制；Redis 降级为可选。原 PostgreSQL+Redis 表述已从 PRD/后台方案/README 中移除 |
| **款式 `price` 价格记录字段（v1.6）** | 品牌方要求款式主数据包含价格（吊牌/参考零售价），但**后端本期不读写、前台不展示**（Non-goals N5 不变）。落地：数据库设计文档 + 建表 DDL 补 `price NUMERIC(10,2)` 列；ORM 暂不映射（建库以 create_all 为准时无此列），启用需模型补列 |

## 六-B、PRD v1.1 变更：导航结构重构

用户追加要求「主导航固定 5 项 + 指定二级菜单」，经一轮澄清后落地：

| 用户原始要求 | 澄清后落地 | 说明 |
|---|---|---|
| 首页 / 产品 / 新闻 / 招聘 / 关于我们 | **产品 / 门店 / 新闻 / 招聘 / 关于我们**，首页由 Logo 进入 | 用户确认"顶掉首页加门店"，保住门店地图这个核心模块 |
| 产品：男装、女装、童装、**热销**款式 | 男装、女装、童装、**热门推荐** | 官网无销量数据，"热销"不成立，改为运营人工打标 |
| 新闻：企业新闻、行业资讯 | 企业新闻、行业资讯 | 原「资讯」改名「新闻」，分类固定两项 |
| 招聘：社会招聘、校园招聘 | 两页**静态介绍**（人才理念 + 岗位方向 + 投递邮箱） | 用户选"仅静态介绍页"，不做职位列表与在线投递 |
| 关于我们：关于**TP**、发展历程、品牌介绍、联系我们 | **关于 FLY**、品牌介绍、发展历程、联系我们 | "TP"确认为笔误；原「品牌」栏目 4 页重组进「关于我们」 |

**连带调整**：门店两页合并为 `/stores` 单页｜系列必填 `category`、款式新增 `is_hot`/`hot_sort`｜新增 `ContactMessage` 留言实体与后台管理｜接口路径 `/api/collections`→`/api/products`、`/api/articles`→`/api/news`｜新增风险 R10/R11/R12 与待确认 Q9/Q10/Q11。

## 七、下一步

1. **评审 PRD** —— 已完成五轮修订：v1.2 评审修复 30+、v1.3 首页视觉对齐、v1.4 后台图片/轮播/文字可配置澄清、v1.5 数据库选型定稿 SQLite、v1.6 款式新增价格记录字段（见上）。PRD 现为 **v1.6 评审修订版**，建议据此定稿。
2. **确认遗留决策** —— 重点看审核报告的 A1（门店详情 URL 语义化，已入 PRD Q12）与 A3（G2/G3 指标回填）。
2. **回填待确认项** —— 第 13.1 节的 8 个问题（品牌品类、价值主张、真实门店清单、工期团队、设计稿情况、库存更新频率、云资源、多语言）
3. **启动 M0** —— 尤其 ICP 备案周期长，建议立即启动
4. 如需，可补充《接口与数据字典》《竞品对比矩阵》《指标体系》《Now-Next-Later 路线图》（本期按确认范围未展开）
