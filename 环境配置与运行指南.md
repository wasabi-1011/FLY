# FLY 品牌官网 · 环境配置与运行指南

> 适用对象：从 GitHub 克隆代码后**第一次运行**，或换机器后重新搭建环境的人。
> 最后更新：2026-09-12 ｜ 覆盖：Windows（主）/ macOS · Linux（附）
>
> ⚠️ 本文件是环境搭建的**唯一权威说明**。根目录 `启动方式.txt` 是早期随手笔记（部分路径已过时），
> 与其冲突时以本文件为准。

---

## 0. 先看这段：为什么 clone 下来直接跑会失败

仓库为了干净，**刻意没有提交**所有本地环境产物（见 `.gitignore`）。所以你拉到的是"源码"，
还缺下面这些东西，必须自己补：

| 缺失项 | 位置 | 是否必须 | 怎么补 |
|---|---|---|---|
| Python 虚拟环境 | `backend/.venv/` | 必须 | 见第 2 或第 3 节 |
| 环境变量文件 | `backend/.env` | 必须 | 从 `.env.example` 复制（第 5 节） |
| 运行数据库 | `backend/fly.db` | 必须 | 跑 `重置数据库.py`（第 6 节） |
| 前端依赖 | `frontend/web/node_modules/` | 必须 | `npm install`（第 4 节） |
| 上传目录 | `backend/uploads/` | 不用管 | 后端启动会自动创建 |

仓库里**已经带上**、可以直接用的：

- `backend/初始数据/初始数据库.db` —— 初始数据快照（`.gitignore` 里做了 `!` 例外，专门为了分发）。
  内含 3 个账号 / 7 个页面 / 首页 4 帧轮播 / 9 条新闻 / 20 家门店 / 5 款式 / 8 系列。
- `backend/requirements.txt` —— 后端依赖清单（已锁版本）。
- `启动.bat` / `start.bat` —— Windows 一键启动脚本（内容相同，任选其一）。

---

## 1. 环境要求（硬性的，不是建议）

| 组件 | 版本要求 | 说明 |
|---|---|---|
| **Python** | **≥ 3.10，推荐 3.11** | 🔴 **低于 3.10 必定启动失败**。代码大量使用 `str \| None`、`Mapped[list \| None]` 这类 PEP 604 注解，3.9 及以下会直接崩（详见第 9 节 Q1） |
| **Node.js** | ≥ 18，实测 v24.15.0 正常 | 只用于前台 Vite 开发服务器与构建 |
| npm | 随 Node 附带 | 实测 v11.12.1 正常 |
| Git | 任意 | 拉代码用 |
| 数据库 | **无需安装** | SQLite 单文件，零运维 |
| Redis | **可选** | 没配就自动降级为内存实现，不影响启动 |
| Conda | 可选 | 只想用 venv 的话完全不用装 |

磁盘占用参考：`node_modules` 约 100–200 MB，`.venv` 约 100 MB。

---

## 2. 方案 A：Python 内置 venv（推荐，默认方案）

### 2.1 装 Python 3.11（已有 3.10+ 可跳过）

先确认版本，看到 `3.10` / `3.11` / `3.12` 以上就可以直接用：

```bat
python --version
```

版本不够时，Windows 上最省事的方式（免管理员权限，装到用户目录）：

```bat
winget install -e --id Python.Python.3.11 --scope user --accept-source-agreements --accept-package-agreements
```

装完解释器通常在：

```
C:\Users\<你的用户名>\AppData\Local\Programs\Python\Python311\python.exe
```

> 也可以去 python.org 下载安装包。安装时**勾选 "Add python.exe to PATH"** 会省事很多。

### 2.2 创建虚拟环境并装依赖

以下命令中的 `D:\FLY网站\FLY` 换成你自己的克隆路径（下文统称**项目根目录**）。

```bat
cd /d D:\FLY网站\FLY\backend

rem 1) 建虚拟环境（把 python 换成你确认过版本的 3.11 解释器）
"C:\Users\<你的用户名>\AppData\Local\Programs\Python\Python311\python.exe" -m venv .venv

rem 2) 激活
.\.venv\Scripts\activate

rem 3) 装依赖（务必确认命令行前缀出现 (.venv)）
python -m pip install --upgrade pip
pip install -r requirements.txt
```

验证环境是否搭对（**这三行必须都能正常输出**）：

```bat
python -c "import sys; print(sys.version)"
python -c "import fastapi, sqlalchemy, uvicorn; print('deps ok')"
```

---

## 3. 方案 B：Conda 环境（可选）

适合本来就用 Anaconda / Miniconda 的人。**用了 conda 就不需要 `.venv`**，两者选一个即可。

### 3.1 创建环境与安装依赖

```bat
rem 若提示 conda 不是内部命令，先初始化一次再重开终端
conda init cmd.exe

rem 1) 建环境（python 版本必须是 3.10+）
conda create -n fly python=3.11 -y

rem 2) 激活
conda activate fly

rem 3) 装依赖
cd /d D:\FLY网站\FLY\backend
python -m pip install --upgrade pip
pip install -r requirements.txt
```

### 3.2 Conda 用户的启动方式

**建议用手动命令启动**（见第 7.2 节），原因：`启动.bat` 查找解释器的顺序是
`backend\.venv` → 用户目录 Python311 → `C:\Program Files\Python311` → PATH 里的 `python`。
只要机器上还装着独立的 Python 3.11，脚本就会跳过你激活的 conda 环境。

如果你确实想用脚本，需满足"本机没有独立 Python 3.11"这一条件，此时脚本最后会退到 PATH，
**在已 `conda activate fly` 的窗口里**启动即可命中 conda 解释器：

```bat
conda activate fly
cd /d D:\FLY网站\FLY
启动.bat
```

### 3.3 常用 conda 维护命令

```bat
conda env list              :: 查看所有环境
conda deactivate            :: 退出当前环境
conda remove -n fly --all   :: 删除 fly 环境（确认不用了再删）
```

---

## 4. 前端依赖安装

```bat
cd /d D:\FLY网站\FLY\frontend\web
npm install
```

装完确认：

```bat
npx vite --version
```

> 国内网络慢可换镜像：`npm config set registry https://registry.npmmirror.com`
> 如果 `node_modules` 是从别的机器直接拷贝过来的，建议仍跑一次 `npm install` 重建 `.bin` 下的可执行脚本。

---

## 5. 首次运行必做：生成 `.env`

`.env` 被 git 忽略，需要自己从示例文件复制。**默认值即可直接跑**，一般不用改：

```bat
cd /d D:\FLY网站\FLY\backend
copy .env.example .env
```

几个可能想改的项（都在 `backend/.env`）：

| 变量 | 默认 | 说明 |
|---|---|---|
| `DATABASE_URL` | `sqlite+aiosqlite:///./fly.db` | 数据库位置，一般不动 |
| `SECRET_KEY` | 占位字符串 | **部署到公网前必须换掉** |
| `CORS_ORIGINS` | `["http://localhost:3000","http://localhost:5173"]` | 换个端口跑前台就要加进去，否则浏览器报 CORS |
| `TMAP_SERVER_KEY` | 空 | 腾讯位置服务的服务端 Key。留空时"地址→坐标"接口返回 501，后台自动降级为地图点选/手填坐标，**不影响门店正常增删改** |

---

## 6. 首次运行必做：恢复数据库

运行库 `fly.db` 不在仓库里，用随仓库分发的初始快照生成：

```bat
cd /d D:\FLY网站\FLY\backend
python 重置数据库.py
```

看到类似输出即成功：

```
pages=7 | blocks=13 | articles=9 | items=5 | collections=8 | admin_users=3
```

> ⚠️ 执行前**必须先停掉后端服务**，否则数据库文件被占用会失败。

其他数据库操作：

```bat
python 重置数据库.py --show       :: 只看当前库/快照状态，不改动
python 重置数据库.py --seed       :: 只重灌 9 条新闻
python 重置数据库.py --snapshot   :: 把当前库另存为新的初始快照（更新基准，慎用）
python 种子数据_商品.py           :: 只重灌款式/系列
```

---

## 7. 启动服务

### 7.1 一键脚本（Windows，最简单）

双击项目根目录的 **`启动.bat`**（`start.bat` 内容相同）：

| 选项 | 作用 |
|---|---|
| `1` | 全部启动：前台 3000 + 后台 8000，并自动开两个页面 |
| `2` | 仅后端 + 后台管理（8000） |
| `3` | 仅前台官网（3000） |
| `4` | 停止全部服务（杀掉占用 8000 / 3000 的进程） |
| `5` | 重置数据库（自动先停服务） |
| `6` | 查看数据库状态 |
| `7` | 只重灌款式/系列种子数据 |
| `0` | 退出 |

> 脚本是 **GBK 编码**，别用编辑器另存为 UTF-8，否则中文菜单乱码、脚本报错。
> 启动时会打印当前使用的 Python 版本；若低于 3.10 会显示 `[WARN] need 3.10+`。

### 7.2 手动启动（conda 用户 / 非 Windows / 想看完整日志）

开**两个**终端窗口：

**窗口 1 —— 后端（8000）**

```bat
cd /d D:\FLY网站\FLY\backend
.\.venv\Scripts\activate              :: conda 用户改为 conda activate fly
uvicorn app.main:app --reload --port 8000
```

**窗口 2 —— 前台（3000）**

```bat
cd /d D:\FLY网站\FLY\frontend\web
npm run dev
```

macOS / Linux 上把激活命令换成 `source .venv/bin/activate`，路径分隔符换成 `/`，其余一致。

### 7.3 访问地址

| 服务 | 地址 |
|---|---|
| 前台官网 | <http://localhost:3000> |
| 后台管理 | <http://localhost:8000/admin/index.html> |
| API 文档（Swagger） | <http://localhost:8000/docs> |
| 健康检查 | <http://localhost:8000/health> |

**后台账号**（密码都是用户名 + `1234`）：

| 账号 | 角色 |
|---|---|
| `admin` / `admin1234` | 超级管理员 |
| `editor` / `editor1234` | 内容运营 |
| `merch` / `merch1234` | 商品门店运营 |

---

## 8. 端口与目录速查

| 端口 | 谁在监听 | 说明 |
|---|---|---|
| `3000` | Vite 开发服务器（前台） | `/api`、`/uploads` 请求会被代理转发到 8000 |
| `8000` | uvicorn（FastAPI 后端） | 同时挂载 `/admin`（后台页面）、`/uploads`（上传图片）、`/pic`（前台图库） |

```
FLY/
├── backend/            后端 FastAPI（8000）
│   ├── app/            源码
│   ├── .venv/          虚拟环境（自建，git 忽略）
│   ├── .env            环境变量（自己从 .env.example 复制）
│   ├── fly.db          运行数据库（自己生成，git 忽略）
│   ├── 初始数据/        初始快照（随仓库分发）
│   ├── uploads/        上传图片（启动自动创建）
│   └── 重置数据库.py    数据库重置工具
├── frontend/
│   ├── web/            前台官网（3000，Vite + React）
│   └── admin/          后台管理前端资源
├── backendManage/      后台管理静态页（后端挂载为 /admin）
├── docs/               PRD、数据库设计等技术文档
├── 启动.bat            一键启动脚本
└── 环境配置与运行指南.md  本文件
```

---

## 9. 常见问题排查

| # | 报错 / 现象 | 原因 | 解决 |
|---|---|---|---|
| Q1 | `TypeError: Unable to evaluate type annotation 'str \| None'` 或 `sqlalchemy.exc.ArgumentError: Could not resolve all types within mapped annotation: "Mapped[list \| None]"` | Python 版本 < 3.10 | 换成 3.10+ 重建环境（第 2 / 3 节）。**别试图改代码兼容**，注解遍布全局 |
| Q2 | `ModuleNotFoundError: No module named 'fastapi'` | 依赖装到了别的解释器，或没激活环境 | 确认命令行有 `(.venv)` / `(fly)` 前缀；用 `python -m pip install` 而不是裸 `pip install` |
| Q3 | `[Errno 10048] error while attempting to bind on address` | 端口被占用 | `netstat -ano \| findstr :8000` 找 PID 后 `taskkill /F /PID <PID>`；或直接 `启动.bat` 选 4 |
| Q4 | `重置数据库.py` 报 `database is locked` / 权限错误 | 后端还在跑，占用了 `fly.db` | 先停后端（脚本选 4 或关掉窗口）再重置 |
| Q5 | 后台登录返回 `422 Unprocessable Entity` | 登录接口收 **表单**格式，不是 JSON | 用 `application/x-www-form-urlencoded` 传 `username` / `password`（Swagger 页面里用表单填即可） |
| Q6 | 前台有图、后台预览 404 | 后台挂在 8000 端口，图片要走后端的 `/pic` 挂载 | 确认 `frontend/web/public/pic/` 存在；后端启动时会检查并挂载，缺失则检查该目录 |
| Q7 | 浏览器打开 `127.0.0.1:3000` 连不上，但 `localhost:3000` 正常 | Node 17+ 默认把 `localhost` 解析到 IPv6 回环，Vite 只监听 `[::1]` | 用 `localhost` 访问即可；确实需要 IPv4 就在 `frontend/web/vite.config.js` 的 `server` 里加 `host: true` |
| Q8 | `npm install` 极慢或超时 | 默认源在国外 | `npm config set registry https://registry.npmmirror.com` |
| Q9 | 装 `bcrypt` 编译报错 / 装完登录异常 | 依赖冲突 | **不要升级 bcrypt**：`passlib 1.7.4` 与 `bcrypt >= 4.1` 不兼容，requirements 已锁 `bcrypt==3.2.2` |
| Q10 | `启动.bat` 中文全是乱码 | bat 被存成了 UTF-8 | 文件必须是 GBK 编码，用 `chcp 936` 环境重写或重新从仓库获取 |
| Q11 | 后台"地址→坐标"返回 501 | 未配置 `TMAP_SERVER_KEY` | 预期行为，已自动降级为地图点选/手工填坐标；需要自动解析就去腾讯位置服务申请 Key 填进 `.env` |
| Q12 | 前台页面空白 / 无数据 | 数据库没初始化 | 跑 `python 重置数据库.py`（第 6 节） |
| Q13 | 浏览器控制台报 CORS | 前台端口不在白名单 | 在 `backend/.env` 的 `CORS_ORIGINS` 里加上你的访问地址，重启后端 |
| Q14 | 改了代码后端没生效 | 没开热重载 | 启动时加 `--reload`： `uvicorn app.main:app --reload --port 8000` |

---

## 10. 全新环境最短路径（复制粘贴版）

```bat
:: 1. 后端环境
cd /d D:\FLY网站\FLY\backend
"C:\Users\<你的用户名>\AppData\Local\Programs\Python\Python311\python.exe" -m venv .venv
.\.venv\Scripts\activate
python -m pip install --upgrade pip
pip install -r requirements.txt
copy .env.example .env
python 重置数据库.py

:: 2. 前端依赖
cd /d D:\FLY网站\FLY\frontend\web
npm install

:: 3. 启动：回到根目录双击 启动.bat 选 1
```

Conda 版第 1 步替换为：

```bat
conda create -n fly python=3.11 -y
conda activate fly
cd /d D:\FLY网站\FLY\backend
python -m pip install --upgrade pip
pip install -r requirements.txt
copy .env.example .env
python 重置数据库.py
```

---

## 11. 上线/部署前提醒

- `backend/.env` 里的 `SECRET_KEY` **必须换成随机长字符串**，不要沿用示例值。
- `.env` 不进版本库，部署环境的 `.env` 需要单独配置（参考 `.env.example`）。
- SQLite 建议开启 WAL 并做每日备份，详见 `docs/FLY品牌官网_数据库设计技术文档_SQLite_v1.0.md`。
- 若接腾讯地图，前端 Key 在 `frontend/web/index.html` 与 `backendManage/index.html` 的
  `window.__FLY_TMAP_KEY__` 注入，且需在控制台配置 Referer 白名单。
